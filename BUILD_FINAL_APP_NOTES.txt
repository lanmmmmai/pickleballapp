FINAL BUILD (best-effort patch)

Added / changed:
- Backend: socket.io server, realtime booking update events.
- Backend: product module (/api/products).
- Backend: class module (/api/classes).
- Backend: booking create + my bookings + checkin + no-show.
- Backend: video feed endpoint (/api/videos/feed), approval endpoints.
- Admin web: Products page, Classes page, realtime Bookings page.
- Flutter: Products screen, Booking history, Classes screens, profile links, vertical video feed screen.

Important before running:
1. Reinstall deps
   backend: npm install
   admin-web: npm install
2. Update database schema
   cd pickleball-backend
   npx prisma generate
   npx prisma migrate dev --name final_build_patch
3. Run backend/admin/flutter again.

Notes:
- Existing Court flow is preserved for compatibility. New Product system is added alongside it.
- TikTok feed is implemented as a vertical full-screen feed UI. For full media playback quality, you may later add youtube/video player plugins if desired.
- Realtime booking emits socket event: booking:update


Follow-up patch (Mar 2026)
- Admin web: fixed lint/blocking issues in Classes, Products, router guard extraction.
- Admin web: corrected COACH label to 'Huấn luyện viên'.
- Backend classes: prevent duplicate enrollments and over-capacity enrollments.
- Repacked project as a new zip for continued handoff.
